# KickIt landing screenshots

The landing at `/kickit/` references the following images. Drop them in
this folder (PNG, 1179×2556 — same as App Store 6.7" screenshots) and
they'll appear automatically on next deploy.

| File | Where it shows |
|------|----------------|
| `hero1.png` | Top hero grid, left tile |
| `hero2.png` | Top hero grid, middle tile |
| `hero3.png` | Top hero grid, right tile |
| `friends.png` | "See who's free, right now" section |
| `map.png` | "Drop a pin. Make a plan." section |
| `vibe.png` | "Two yeses → real hangout." section |
| `groups.png` | "Groups for the regulars." section |

Generate via `screenshot-kit` → `swift run KickItShots` once the screenshots
target is wired (mirrors what's already there for Speaky / Commercials).
